<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.testa.ContactMainActivity" >

    <!--
     	android:groupIndicator="@null" 取消默认图片 
    	android:childIndicatorLeft 设置孩子左边间距 
    	android:dividerHeight 这个高度一定要设置，不然显示不出来分割线，估计默认为0 吧 
     	android:childDivider="@drawable/child_bg" 这个直接引color,或者图片会导致整个孩子背景都为这个颜色  ，不知道原因，如果有谁知道，请Give me say. 
    -->

    <ExpandableListView
        android:id="@+id/jg_expandablelistview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cacheColorHint="@null"
        android:childDivider="@drawable/bg_address_list_press"
        android:childIndicatorLeft="5dp"
        android:divider="@drawable/bg_line"
        android:dividerHeight="1dp"
        android:groupIndicator="@null"
        android:listSelector="@null"
        android:scrollbarAlwaysDrawHorizontalTrack="true" >
    </ExpandableListView>

</RelativeLayout>